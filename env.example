# Main domain
MAIN_DOMAIN=yourdomain.com

# Timezone
TZ=Europe/Madrid

# Cloudflare Tunnel
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# Cloudflare API (for ACME DNS challenge)
CF_API_EMAIL=your_cloudflare_email@example.com
CF_API_KEY=your_cloudflare_api_key_here
CF_DNS_API_TOKEN=your_cloudflare_dns_api_token_here

# ACME Email for Let's Encrypt certificates
ACME_EMAIL=your_email@example.com

# Traefik Configuration
TRAEFIK_LOG_LEVEL=INFO
TRAEFIK_HTTP_PORT=80
TRAEFIK_HTTPS_PORT=443
TRAEFIK_DASHBOARD_ENABLED=true
TRAEFIK_API_INSECURE=false

# Traefik Basic Auth (generate with: htpasswd -nb admin password)
# Format: user:hashed_password (multiple users separated by commas)
TRAEFIK_BASIC_AUTH=admin:$$apr1$$hashed_password_here

# AdGuard Home Configuration
ADGUARD_DNS_IP=192.168.0.100
ADGUARD_DNS_PORT=53
ADGUARD_HTTP_PORT=8050
ADGUARD_HTTPS_PORT=4430
ADGUARD_ADMIN_PORT=3000
ADGUARD_DOT_PORT=853
ADGUARD_DOH_PORT=784
ADGUARD_QUIC_PORT=8853
ADGUARD_TLS_PORT=5443
ADGUARD_DHCP_SERVER_PORT=67
ADGUARD_DHCP_CLIENT_PORT=68

# Storage Services (PostgreSQL y Redis compartidos)
# PostgreSQL - Generate with: openssl rand -base64 36
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgresql_password_here
POSTGRES_DB=shared
# Cada servicio puede usar su propia base de datos (Authentik la creará automáticamente)
# O pueden compartir POSTGRES_DB usando diferentes esquemas

# Redis - Generate with: openssl rand -base64 36
REDIS_PASSWORD=your_redis_password_here

# MinIO - S3-compatible storage (self-hosted)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=your_minio_password_here
MINIO_BROWSER=on
MINIO_DOMAIN=s3.yourdomain.com

# Authentik Configuration
# Generate with: openssl rand -base64 60
AUTHENTIK_SECRET_KEY=your_authentik_secret_key_here
# Base de datos específica para Authentik (debe estar en POSTGRES_DATABASES)
AUTHENTIK_PG_DB=authentik
# Optional: Email configuration (for password resets, etc.)
AUTHENTIK_EMAIL__HOST=smtp.example.com
AUTHENTIK_EMAIL__FROM=authentik@example.com
AUTHENTIK_EMAIL__USERNAME=smtp_username
AUTHENTIK_EMAIL__PASSWORD=smtp_password
AUTHENTIK_EMAIL__USE_TLS=true
AUTHENTIK_EMAIL__USE_SSL=false
AUTHENTIK_EMAIL__PORT=587

# FreshRSS Configuration
FRESHRSS_BASE_URL=https://rss.yourdomain.com
FRESHRSS_CRON_MIN=*/20  # Actualización feeds: */20 (cada 20min), 3,33 (cada 30min), 7 (cada hora)
FRESHRSS_DEFAULT_USER=admin
FRESHRSS_ADMIN_EMAIL=your_email@example.com
FRESHRSS_ADMIN_PASSWORD=your_admin_password_here
FRESHRSS_ADMIN_API_PASSWORD=your_api_password_here  # Para apps móviles
FRESHRSS_PG_DB=freshrss  # Base de datos en PostgreSQL compartido

# SplitPro - Gestión de gastos compartidos
SPLITPRO_BASE_URL=https://split.yourdomain.com
SPLITPRO_PG_DB=splitpro
SPLITPRO_NEXTAUTH_SECRET=your_nextauth_secret_here
SPLITPRO_DEFAULT_HOMEPAGE=/balances
SPLITPRO_ENABLE_INVITES=true
SPLITPRO_DISABLE_EMAIL_SIGNUP=false

# SplitPro - Storage (MinIO local)
SPLITPRO_R2_ACCESS_KEY=admin
SPLITPRO_R2_SECRET_KEY=your_minio_password_here
SPLITPRO_R2_BUCKET=splitpro
SPLITPRO_R2_URL=http://storage-minio:9000
SPLITPRO_R2_PUBLIC_URL=https://s3.yourdomain.com/splitpro

# SplitPro - OAuth (al menos uno requerido)
SPLITPRO_GOOGLE_CLIENT_ID=your_google_client_id_here
SPLITPRO_GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# O usar Authentik:
# SPLITPRO_AUTHENTIK_ID=your_authentik_client_id
# SPLITPRO_AUTHENTIK_SECRET=your_authentik_client_secret
# SPLITPRO_AUTHENTIK_ISSUER=https://auth.yourdomain.com/application/o/splitpro

# SplitPro - Opcionales
SPLITPRO_CURRENCY_PROVIDER=frankfurter
SPLITPRO_CACHE_CRON=0 2 * * 0
SPLITPRO_CACHE_RETENTION=2 days

